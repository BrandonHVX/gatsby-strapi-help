/**
 * Given a product slug, return the relative path to single product page
 */
export const fromProductSlugToUrl = (slug) => `/products/${slug}`
export const fromDeliverySlugToUrl = (slug) => `/pickup_delivery/${slug}`